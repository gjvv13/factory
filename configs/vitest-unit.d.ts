import type { UserConfig } from 'vitest/config';

/** Vitest-preset voor unit tests. Zie vitest-unit.js voor de inhoud. */
export declare function unitTestConfig(overrides?: Record<string, unknown>): UserConfig;
export default unitTestConfig;
